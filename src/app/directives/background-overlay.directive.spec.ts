import { BackgroundOverlayDirective } from './background-overlay.directive';

describe('BackgroundOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
